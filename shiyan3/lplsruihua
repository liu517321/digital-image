%拉普拉斯掩模锐化
I=imread('E:\matlab\tuxiang\实验三\实验3\模糊图像（用于锐化）\dayanta_mohu.bmp');
figure(1)
subplot(1,2,1);
imshow(I);
title('原始图像');
I=double(I);
H=[0,1,0;1,-4,1;0,1,0];%拉普拉斯算子
J=conv2(I,H,'same');
K=I-J;
subplot(1,2,2);
imshow(K,[]);
title('拉普拉斯锐化后图像');
